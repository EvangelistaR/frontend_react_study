/**
 * babel: Converter (transpilar) código do React p/ um código que o browser entenda
 * webpack: P/ cada tipo de arquivo (.js, .css, .png) converter o código de uma maneira diferente
 * 
 * loaders: babel=loader, css=loadar, image=loader
 */